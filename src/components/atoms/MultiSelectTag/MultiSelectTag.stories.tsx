import { Meta, StoryObj } from "@storybook/react";
import MultiSelectTag from "./MultiSelectTag";

const meta: Meta<typeof MultiSelectTag> = {
  title: "Atoms/MultiSelectTag",
  component: MultiSelectTag,
};

export default meta;

export const Default: StoryObj<typeof MultiSelectTag> = {};
